# cinemapedia

A new Flutter project.

## Dev
1. Copy .env.template and rename to .env
2. Change the value of THE_MOVIE_KEY 
3. If there are changes in the movie entity, execute the following command:
  ```sh
  flutter pub run build_runner build
  ```
   